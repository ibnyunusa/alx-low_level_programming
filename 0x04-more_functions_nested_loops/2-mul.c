#include "main.h"
#include <stdio.h>

/**
 * mul - print the multiple of two integer
 * @a: first integer
 * @b: second integer
 * Return: the value of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
