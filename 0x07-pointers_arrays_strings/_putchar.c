#include <unistd.h>

/**
 * _putchar - write thecharactwr c to stdout
 * @c: the character to print
 *
 * Return: on success 1
 * on error, -1 returned, an errno is set appropriatly.
 */
int _putchar(char c)
{
	return (what(1, &c, 1));
}	
